<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - Sistema de Gestión de Notas</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Chart.js -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <!-- Custom CSS -->
    <link href="/css/style.css?v=1.2" rel="stylesheet">
    <!-- Fix CSS -->
    <link href="/css/fix.css?v=1.0" rel="stylesheet">
</head>
<body>
    {{#if user}}
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand" href="/dashboard">
                <i class="bi bi-mortarboard-fill me-2"></i>
                Sistema de Notas
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/dashboard">
                            <i class="bi bi-house-door me-1"></i>Dashboard
                        </a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <i class="bi bi-people me-1"></i>Estudiantes
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/students">Ver Estudiantes</a></li>
                            <li><a class="dropdown-item" href="/students/import">Importar CSV</a></li>
                        </ul>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <i class="bi bi-journal-text me-1"></i>Calificaciones
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/grades">Gestionar Notas</a></li>
                            <li><a class="dropdown-item" href="/grades/import">Importar Notas</a></li>
                        </ul>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/reports">
                            <i class="bi bi-graph-up me-1"></i>Reportes
                        </a>
                    </li>
                    {{#if (eq user.role "admin")}}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <i class="bi bi-gear me-1"></i>Administración
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/schools">Escuelas</a></li>
                            <li><a class="dropdown-item" href="/users">Usuarios</a></li>
                        </ul>
                    </li>
                    {{/if}}
                </ul>
                
                <ul class="navbar-nav">
                    {{#if school}}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <i class="bi bi-building me-1"></i>{{school.name}}
                        </a>
                        <ul class="dropdown-menu">
                            <li><h6 class="dropdown-header">Cambiar Escuela</h6></li>
                            <li><a class="dropdown-item" href="#" id="switchSchoolBtn">Seleccionar otra escuela</a></li>
                        </ul>
                    </li>
                    {{/if}}
                    
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <i class="bi bi-person-circle me-1"></i>{{user.first_name}} {{user.last_name}}
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/profile">
                                <i class="bi bi-person me-2"></i>Perfil
                            </a></li>
                            <li><a class="dropdown-item" href="#" id="changePasswordBtn">
                                <i class="bi bi-key me-2"></i>Cambiar Contraseña
                            </a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/auth/logout">
                                <i class="bi bi-box-arrow-right me-2"></i>Cerrar Sesión
                            </a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    {{/if}}

    <!-- Main Content -->
    <main class="{{#unless user}}auth-container{{/unless}}">
        {{#if user}}
        <div class="container-fluid mt-4">
        {{/if}}
            
            <!-- Alertas -->
            <div id="alertContainer"></div>
            
            <!-- Contenido de la página -->
            {{{body}}}
            
        {{#if user}}
        </div>
        {{/if}}
    </main>

    <!-- Footer -->
    {{#if user}}
    <footer class="bg-light text-center text-muted py-3 mt-5">
        <div class="container">
            <p class="mb-0">&copy; {{currentYear}} Sistema de Gestión de Notas Escolares. Todos los derechos reservados.</p>
        </div>
    </footer>
    {{/if}}

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Custom JS -->
    <script src="/js/app.js"></script>
    
    <!-- Scripts específicos de la página -->
    {{#if scripts}}
        {{#each scripts}}
        <script src="{{this}}"></script>
        {{/each}}
    {{/if}}
</body>
</html>


